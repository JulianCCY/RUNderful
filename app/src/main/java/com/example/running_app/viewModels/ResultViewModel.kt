package com.example.running_app.viewModels

import androidx.lifecycle.LiveData
import androidx.lifecycle.ViewModel
import com.example.running_app.data.db.RoomDB
import com.example.running_app.views.RunRecordForUI

class ResultViewModel: ViewModel() {

//    private val db = RoomDB.get(application)
//    fun getSummary(): LiveData<RunRecordForUI> = db.?dao,get()???

// get the last inserted record? cuz no confirm button and u already saved while "finish" was pressed
    // so no need to pass data from running screen
}